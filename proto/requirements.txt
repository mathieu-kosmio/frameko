# Frameko — proto V1
# Accès base de données Supabase / Postgres
supabase>=2.10
psycopg[binary]>=3.2
psycopg-pool>=3.2

# Configuration (lecture de .env, jamais de secret en dur)
python-dotenv>=1.0

# Chargement de l'ontologie
rdflib>=7.1
pandas>=2.2

# Embeddings locaux (modèle multilingue 384 dim, aucune clé API externe) via ONNX léger.
# Backend par défaut = fastembed. Alternative sentence-transformers (tire torch, ~2 Go) :
#   pip install "sentence-transformers>=3.0"  +  EMBEDDING_BACKEND=sentence-transformers
fastembed>=0.4

# Serveur MCP
fastmcp>=2.0

# Sessions de l'UI web (auth par jeton d'organisation)
itsdangerous>=2.2

# Vérification des JWT Supabase (mode connecté : auto-évaluation par documents)
PyJWT>=2.9

# Ingestion IA — optionnel (uniquement pour scripts/ingest_framework.py --llm)
openai>=1.50

# Extraction de sources (scripts/extract_source.py)
pypdf>=5.0
openpyxl>=3.1

# Tests
pytest>=8.0
